node_modules
dist
dist-ssr
dist-types
*.tsbuildinfo
*.local
.env
.env.local
.DS_Store
.vscode/*
!.vscode/extensions.json
npm-debug.log*
.netlify

# Deployable bundles built for drag-and-drop deploys (regenerate with vite build)
*.zip

# Verification screenshots from QA runs — artifacts, not source.
qa-*.png
